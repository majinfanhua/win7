# TODO

updated: 2026-09-17（第四轮：超时改停顿判定 + 气泡换行清理 + zip 套文件夹）

## 第四轮已完成（2026-09-17）

用户报的三个问题，逐条对应：

| 报的问题 | 根因 | 修法 |
|---|---|---|
| ✅ **zip 解压摊一地** | electron-builder 的 zip 目标在 Windows 上写死平铺（`ArchiveTarget` 的 `withoutDir = !isMac`），配置里没有「包一层目录」的选项 | 新增 `scripts/zip-wrap-folder.mjs`，挂 `afterAllArtifactBuild`，**纯 Node 直接改 zip 字节**把顶层条目挪进 `hangkeIDE-<version>-...-<arch>/`。CI 加一条断言开箱验结构 |
| ✅ **AI 明明在输出却因 120s 结束** | `streamRound` 里的 `setTimeout(120_000)` 覆盖**整次 HTTP 往返**（建连 + 等首字 + 输出），是「总时长上限」而不是「多久没动静才算卡死」。每轮确实都重置 —— 问题不在重置，而在每轮给的是总时长 | 改成停顿看门狗（`src/main/stream-watchdog.ts`）：每收到一片数据重置（5 分钟）+ 单轮硬上限 15 分钟兜底 |
| ✅ **改代码时因超时被中断** | 同上。要调 `editFile` 的那一轮，模型得先吐完思维链与正文、再分片吐工具调用参数，往返超过 120s 就在参数吐完前被掐断。（**工具执行本身不在计时范围内** —— 那时这一轮已 settle、表已撤） | 同上 |
| ✅ **气泡无效换行**（每执行一次工具就多一片空白） | 一次回答由好几轮拼成，每轮正文前后模型都带换行，直接拼进同一个气泡 | `shared/chat-text.ts` 的 `normalizeChatText`，在 `MessageBubble` 渲染前收口 |

**新增的三道护栏**（都进了 `npm run build` 与 CI）：

- `scripts/check-idle-timeout.mjs` —— 超时必须仍是停顿判定。
  光把常量改大不算修好：如果 SSE 的 `data` 回调里没有 `progress()`，
  那仍然是一个「每轮固定 N 分钟」的定时炸弹。所以同时断言阈值、重置点、停表点。
- `scripts/check-chat-text.mjs` —— 换行清理的三条规则
  （只压连续空行 / 只动行尾 / 幂等）。错了只是空白多寡的差别，界面上很难看出来。
- `scripts/check-zip-wrap.mjs` —— zip 字节级改写的 16 项断言。
  测试用的 zip 由脚本自己按规范拼出来（只用 `node:zlib`），
  所以不依赖 electron-builder、不依赖 7za、也不依赖产物已存在，**打包前就能跑**。

**这一轮踩的坑（重要）**：

zip 套文件夹的第一版实现调外部 `7za rn` 并解析它的**文本输出**拿文件名，
**在 CI 上挂了**。两个原因都在开发机上测不到：

1. 外部 7za 的版本行为 —— 开发机是 Linux p7zip **16.02**，
   runner 是 Windows 7-Zip **21.07**，不是同一个程序。
   **本项目只出 Windows 包，拿 Linux 的 7za 验证等于没验证。**
2. 7za 按**控制台代码页**输出文件名，英文 runner 表示不了「使用说明.txt」，
   读回来是乱码，再喂回 `rn` 就匹配不到 —— **而匹配不到时退出码仍是 0**，
   静默什么都没做。

现在改成纯 Node 直接改 zip 字节：零外部进程、不解析任何文本输出，
两边行为完全一致。**以后凡是「给 Windows 用的东西」，别拿 Linux 的同名工具
当验证依据** —— 这条对打包链路尤其重要。

**这一轮没在真机上验过的**：
- ⬜ zip 套文件夹在本地用**真实 Release 产物**验过（顶层 19 项 → 1 项、
  75 个条目的 CRC / 大小 / 压缩方法 / 时间戳 / UTF-8 标志位逐条一致、
  内容 SHA256 全等，并用 python zipfile 与 7za 两条独立路径复验）。
  现在实现是纯 Node、无平台差异，但**仍以 CI 那一步的实际结果为准**。
- ⬜ 停顿看门狗只做了静态断言与构建验证，**没有真的挂一个慢模型跑满 5 分钟**。
  想手工验：把 `STREAM_STALL_TIMEOUT_MS` 临时改成 10 秒，发一个长问题，
  看是不是「只要还在吐字就不超时」。

## 第三轮已完成（2026-09-15）

| 项 | 说明 |
|---|---|
| ✅ **命令执行改用 cmd.exe** | **Win7 从 6 个工具变 12 个**（原来 Win7 分支写死 `commandExec=false`，根本没查解释器在不在）|
| ✅ **Glob / Grep** | 跨系统，Win7 也能用。自写 glob 匹配，不引 fast-glob / ripgrep |
| ✅ **拆 useAppStore** | 1100 行 → session(196) / tree(407) / editor(328) 三个 slice |
| ✅ **`!` + Tab 片段补全** | 与「新建文件套模板」共用同一份数据 |
| ✅ **图片输入** | 粘贴或选文件 → 压到长边 1568px → 多模态。仅当模型开启图片支持 |
| ✅ **内嵌预览面板** | iframe + 已有静态服务，改完自动刷新 |
| ✅ **运行时探测** | 探测 python / node / git，进 system prompt 让 AI 不再试错 |

**未做（明确决定）**：内置截屏。学生用 `Win+Shift+S` / QQ / 微信截图后
`Ctrl+V` 粘贴已经完全覆盖，而内置截屏要写全屏遮罩 + 拖框选区 +
多显示器判断，成本远高于收益。「附件」（任意文件）入口也保持置灰。

## 第二轮已完成（2026-09-15）

一次「把所有没做完的补上、把 bug 修掉」的收尾。逐项列出来是为了让后来者
知道**哪些坑已经填过**，不要重复劳动，也不要以为它们还在。

- ✅ **文件树移动**（原第 0 条，见下）—— 拖拽 + 右键「移动到…」两条路径
- ✅ **`DoctorDialog` 接线** —— 菜单「帮助 → 环境体检」以前点了没反应
- ✅ **日志有了消费者** —— 新增 `LogDrawer` 底部抽屉 + `onLog` 订阅。
  这条修的是一个**静默失效的安全机制**：`handleFileChanged` 在「AI 改了文件
  但编辑器里有未保存改动」时只 pushLog 一条 warn，而那条 warn 以前没人显示
- ✅ **dirty 三道守卫** —— 关标签 / 换项目 / 关窗口都会拦
- ✅ **删除改 `shell.trashItem`** —— 原来往工作区 `.trash` rename（看不见也拿不回）
- ✅ **`forgetSubtrees`** —— 目录删除/改名/移动后清 `childMap` 子树（幽灵文件）
- ✅ **会话切换时中断在飞的流** —— 否则回答尾段会落进另一条会话
- ✅ **`parentOf` / `parentDirOf` 合并** —— 曾经两份实现并存
- ✅ **`RefRow` 提取** —— AiPanel 里 34 行 JSX 原样复制了两份
- ✅ **`ts.worker` 懒加载** —— 9.6 MB 改成按需（纯 HTML 项目不再付这个代价）
- ✅ **`SPLIT_MIN/MAX` 统一** —— 界面 0.28/0.78 vs 落盘 0.2/0.9 的不一致
- ✅ **README 漂移修正** —— 分割条「改/均/问」按钮、回收站说法

## 下一步（按优先级）

### 0. ~~文件树：拖拽移动~~（已完成 2026-09-15）

已做：主进程新增 `wsMove`，拖拽（`TreeNode` 的原生 DnD）与右键「移动到…」
（`MoveDialog`）两条路径共用 `store.moveEntry`。设计要点见 `claude.md`
的「拖拽移动」一节。留下的注意点：

- **重名是报错不是覆盖**（`wsMove` 显式探一次）。覆盖等于无声删掉学生另一个文件，
  比删除更糟 —— 删除至少还进回收站。
- **跨盘回退 `copy + unlink`** 用的是 Node 16.7+ 自带的 `fsp.cp`，
  没有引入 `fs-extra`（本项目零运行时依赖，别破这个约束）。
- **目录环拦两处**：渲染层 `isDescendantOf` 让非法落点不高亮，
  主进程 `wsMove` 是权威守卫（渲染层路径不可信）。两处都要留。
- **未验证**：跨盘（`EXDEV`）回退路径与「目标只读」分支都只在代码里走过，
  真机上没构造过。分发前拿 U 盘或第二个分区试一次。

### 0.5 `useAppStore.ts` 已 1100 行，超标（新增，优先级高）

`claude.md` 第 3 条的红线是 800 行，本轮加了三块逻辑后到 1100 行。
**下次动这个文件之前先拆**，建议按「会话 / 文件树 / 编辑器标签」切成三个 slice ——
这三块之间几乎不互相调用，拆起来阻力不大。
再往里塞东西会让「改一处不知道会影响谁」的风险继续涨。

### 1. ~~`Glob` / `Grep`~~（已完成 2026-09-15）

两个都做了，`apply_patch` 仍未做。设计要点见 `claude.md`
「其他本轮新增的模块」一节。留下的注意点：

- **没引 minimatch，自己实现了 `compileGlob`**。项目约定是零运行时依赖
  （所有依赖在 devDependencies），而 minimatch 目前只是构建工具的传递依赖，
  **不会进打包产物** —— 引它等于新增一个运行时依赖。
  支持的语法：`*` `?` `**` `[abc]` / `[!abc]`。18 条语义用例已验证过
- **`{a,b}` 大括号展开不支持，且明确报错**。静默当字面量匹配会得到
  莫名其妙的结果，比报错更难查
- **撞上限时必须在结果里说明**（「已达上限，可能还有遗漏」）。
  不说的话模型会把「没搜完」当成「不存在」—— 这是搜索工具最危险的失败模式
- 两个工具共用一套 `walk` 骨架，所以 `SKIP_DIRS` 只有一份，
  且与文件树的 `ALWAYS_IGNORED` 保持一致

`apply_patch` 仍缓做：它要求模型输出结构化 diff，而现有的
`editFile` / `multiEdit` 已经覆盖了「改几处」的场景，
收益不明显而实现与调试成本都不低。

### 2. 命令类工具的真机验证

见 `docs/多系统-测试清单.md` 第六节新增的 6 项（中文编码 / 非 0 退出码 / 超时 / 后台任务 / 残留进程）。
开发机是 Linux，`powershellPath()` 返回 null，**PowerShell 那条真实执行路径一行都没跑过**。
首次上 Windows 时重点看：`node -v` 能不能返回、`echo 你好` 会不会乱码、超时是不是真的杀掉了子进程。

### 2.5 文件树这一轮的遗留（已完成 2026-09-15）

「工作空间只能建文件夹、不能建文件」已修。改的地方与理由见 `claude.md` 的
「文件树 / 资源管理器」一节。留下的注意点：

- **`sidebar.css` 里 `.tree-badge` 的 tone 配色与 `file-tree/shared.ts` 的
  `fileBadge()` 是两份**（CSS 管颜色、TS 管文字）。加新扩展名要同时改两处，
  否则会出现「有短标签但没颜色」。
- **`.filetree` 类名被两种形态共用**。`responsive.css` 里 `@media (max-width:900px)`
  把 `.filetree` 设成 `display:none` 是为了收掉侧栏那份，会把资源管理器里的树一起干掉；
  已经用 `.filetree:not(.is-embedded)` 显式救回来，**改那条 media 时要一起看**。
- **排序偏好与显示隐藏文件都要能落盘**。`config.explorer` 走的是
  `normalizeExplorer()` 白名单，新加字段忘了进白名单就是「改完重启就没了」。
- 未验证：新建文件时写入模板的失败分支（如目标目录只读）只在代码里走了 `catch`，
  没有在真机上构造过。首次上 Windows 时可以拿只读目录试一次，
  确认弹层里会显示中文原因而不是静默关掉。

### 3. ~~`global.css` 拆分~~（已完成 2026-09-15）

已拆成 `styles/` 下的**十二份**（第二轮加了 `logs.css`），入口 `index.css`，
最长 `sidebar.css` 618 行。清单与顺序约束见 `claude.md` 的「样式」一节。
拆完用逐选择器的声明序列做过等价比对（295 个规则键全一致），
自检三档的 `domNodes` 与拆分前一样。

剩下的事：`dormant.css`（原 global.css 里「暂时不渲染的界面」那一段）成分是混的 ——
编辑器 / 标签栏 / 状态栏的类其实在用（`.editor-host` 还会被 editor.css 再改一次），
**`.logs` 现在也真的上了**（第二轮接的 `LogDrawer`，它在 logs.css 里被装进抽屉外壳）。
只有「输出 / 体检」那几组还是真的没上 —— 体检弹层虽然接线了，但它用的是
`.overlay` / `.dialog` / `.report`，`.report` 确实在 dormant.css 里。
重新审一遍，把还在用的挪到对应文件，剩下的才配叫 dormant。

### 4. 窄屏布局回归

自检里所有几何断言都是在「窗口 1440x900」下成立的。CI runner 屏幕只有 1024x768，
系统会把窗口夹窄到 1024，对话面板跟着变窄 —— 2026-09-15 就因为断言「三个入口必须在同一行」
而误报（`.quick-starts` 本来就写着 `flex-wrap: wrap`）。

已把那条断言改成「允许换行，但第一行至少两张、行内从左往右」。
但**其他几何断言仍然隐含「窗口足够宽」这个前提**，比如 `editorDockHasWidth`（>120px）、
`bothFullHeight`。学生机常见分辨率是 1366x768，比 runner 宽，暂时安全。

要做的事：给自检加一个可控的窗口宽度（如 `--window-size=WxH`），把关键几何断言在
「窄屏」下再跑一遍。现在是靠 runner 的屏幕尺寸“顺便”测到的，不稳定也不自觉。

---

## 已知坑（改之前务必看一眼）

- **不要先 `child.kill()` 再 `taskkill`**。先杀 powershell 就找不到它的子进程了，`/T` 遍历不到，
  反而留下一堆占着端口与 CPU 的孤儿进程。正确顺序写在 `tools/exec.ts` 的注释里。
- **命令输出的编码**：中文 Windows 默认输出代码页是 936（GBK）。
  脚本前必须 `[Console]::OutputEncoding = UTF8`，收字节必须用 `StringDecoder`——
  一个汉字 3 字节，会被切在两个 chunk 之间，`chunk.toString()` 会各自解出半个字符。
- **传脚本用 `-EncodedCommand`，不用 `-Command`**。Node 的引号转义是 C 运行时那套，
  PowerShell 是另一套，引号 / `$` / 反引号混在一起时两边对不上，会出现「在终端能跑、在这里跑不了」。
- **Electron 里 `window.prompt` 不可用**。不是「不弹框」，是调用直接抛错、返回 undefined ——
  所以代码写得再对也什么都不会发生。文件树的新建 / 重命名曾经全依赖它，
  表现就是「点新建文件夹能建（走系统对话框）、点新建文件没反应」。
  现在统一走 `components/InputDialog.tsx`。**`alert` / `confirm` 是可用的，只有 `prompt` 被移除**，
  别因为这次踩坑把 confirm 也一起废掉。
- **`config.ts` 的 `normalize()` 是白名单式的**。新增顶层 section 不加进去，
  每次加载会被静默吞掉（设置里改完、重启就没了，而且不报错）。这是最容易漏的一处。
  `explorer` 段现在走 `normalizeExplorer()`，里面 `sortBy` 是枚举白名单式的 ——
  增删排序方式时两处都要改。
  **同理写 `prompt` 的修正时注意**：`pushLog` 里的 `time: ''` 是刻意的，
  时间戳由 store 统一补，不要传 `new Date()`。
- **`setConfig(patch)` 顶层是浅合并**。传 `{ capability: { mode: 'full' } }` 会把整个 `capability` 替掉。
- **分割条不能占布局宽度**。两侧宽度加起来已经是 100%，分割条再要 9px 会被挤到容器最右边缘（实测错位 454px）。
  解法是 `width:9px + margin:0 -4.5px`；而且 DOM 顺序必须是「编辑器 → 分割条 → 对话」，
  位置由顺序决定，光靠 CSS 救不回来。
- **文件树与侧栏要分两段滚**。`overflow-y:auto` 放在整个侧栏上，会出现「文件多了把侧栏整个推长，
  而文件树自己那一段反而没滚动条」。
- **AI 改了文件、学生手里有未保存改动时绝不覆盖**。只警告，由学生决定——
  把磁盘内容盖上去等于把学生刚写的代码删了。
  ⚠️ **配套前提：那条警告得有地方显示**。本轮之前它只进 `store.logs`，
  而渲染层从来没渲染过 logs —— 警告等于没发。现在靠 `LogDrawer` 承接。
  **再动日志链路时记得这条**：一个没人看的警告比不警告更糟，
  因为它让「已经处理好了」变成一句空话。
- **`closeTab` 与 `closeTabChecked` 不能合并**。前者被「文件已删除」「重命名」
  这些非用户主动路径调用，那些路径上弹「要保存吗」是纯干扰；
  后者才是「用户点了 ×」用的。同理 `forgetSubtrees` 必须挂在
  **删 / 改名 / 移动目录**三条路径上，漏一条就出幽灵文件。
- **单向推送的通道两端都要有人**。`evtLog`（`onLog`）与 `sendMenu` 的动作
  都踩过「主进程发了、渲染层没接」——表现是点了没反应，且不报错。
  新增任何 `evt*` 或菜单动作时，顺手确认对面有消费者。
- **工具写入要标记来源**（`markToolWrite`），否则编辑器分不清「AI 改的」与「别人改的」，只能一律弹提示。
- **文件监视不引 chokidar**。它在 Win7 上会退回轮询模式，50ms 一轮 stat 整棵树，
  机械盘上直接 100% 占用。用原生 `fs.watch`（Windows 上底层是 `ReadDirectoryChangesW`）。
- **样式文件的层叠顺序是契约**，写在 `styles/index.css`。`responsive.css` 必须最后
  （窄屏要覆盖各组件写死的宽度），`editor.css` 必须在所有拆分文件之后
  （`.editor-host` / `.crumb-model` 在前面有基础定义）。`base.css` 最前。
  `logs.css` 必须在 `dormant.css` 之后（那份里已有一个 `.logs`）。
  调顺序不会报错，只会静默地让覆盖失效 —— 而且只在窄屏 / 空态这种边角下看得出来。
- **`SPLIT_MIN` / `SPLIT_MAX` 只有一份**（`shared/types.ts`），界面与落盘都读它。
  曾经界面写 0.28/0.78、落盘夹 0.2/0.9，表现为「拖到中间地带时，
  当次会话被夹住、重启后又原样读回来」——同一次拖动两种行为。
  **别再把字面量写回 `App.tsx`**。
- **中英两个 `parentOf` 只留一个**。现在统一用 `explorer-helpers` 的 `parentDirOf`
  （useAppStore 里 `as parentOf` 重命名导入）。历史上两份实现并存过，
  改其中一份不会报错，只会在某个调用点上算出不同的父目录。

---

## 已验证 / 未验证

已验证（2026-09-15 第二轮，本地静态检查）：

- ✅ `npm run typecheck`（主进程 + 渲染层两份 tsconfig）
- ✅ `npm run check:node16`（23 个文件）与 `npm run check:watch`（6 项全过）
- ✅ `npx electron-vite build` 成功；产物里 `ts.worker` 的 9.6 MB 已不在主 chunk
  的引用链上（只剩一个 0.2 kB 的懒加载壳）
- ⚠️ **`npm run smoke` 本轮没跑成**：本机的沙箱不让 Chromium 写 `/dev/shm`
  与工作区外的 userData 目录，属于环境限制而非代码问题。
  **首次真机 / 有权限的环境上要补跑一次**，重点看本轮新增的两道自检门：
  `logDrawerOk`（日志抽屉能开能关）与 `splitterRangeConsistent`（分割上下限一致）。

已验证（2026-09-15，Linux + xvfb，上一轮）：

- ✅ `npm run typecheck` / `npm run build`（含 `check:node16` + `check:watch`）/ `npm run smoke`
- ✅ 自检跑了三档：默认 / `--capability-profile=win7` / `--capability-profile=win10`，
  能力集与预期一致（本机无 PowerShell，三档都是 6 个，命令类 4 个报「本机不支持」）
- ✅ 新增两条自检断言：不再有「尚未实现」的工具；命令类工具必须跟着探测结果走
- ✅ 样式拆分（global.css 2128 行 → styles/ 十份，最长 599 行）：
  逐选择器的声明序列等价比对 295/295 一致；三档自检 `domNodes` 与拆分前同为 228

已验证（2026-09-15，Windows Server 2022，CI 的注解）：

- ✅ **Win10 上 10 个工具全都生效**：`detected={commandExec:true,backgroundJobs:true}`，
  `effective(10)=readFile,writeFile,editFile,multiEdit,listDir,undoSnapshot,runCommand,jobRun,jobPoll,jobKill`，
  `filtered=` 空。门控「设置 ∩ 探测」在真实 Windows 上算得对。

未验证：

- ⬜ **PowerShell 真实执行路径**——上面「10 个工具生效」只证明工具进了工具表，不等于 `runCommand` 真能跑起来。
  `exec.ts` 那套 `-EncodedCommand` / 编码 / 杀进程树一次都没在 Windows 上执行过，见上面第 2 条。
- ⬜ 杀软（360 等）拦截子进程——校园机器上的典型表现是「偶发失败」，只能真机验
- ⬜ **本轮新增的交互全部没在真机上点过**（沙箱跑不了自检）：拖拽移动、
  「移动到…」弹层、dirty 三处守卫、日志抽屉、体检弹层。
  其中两处最值得优先手工验，因为它们的失败是**无声**的：
  1. **拖拽落点高亮**：`dragover` 漏了 `preventDefault` 的表现就是「拖过去没反应」，
     不报错、不高亮，很容易被当成「拖拽功能没做」
  2. **`shell.trashItem` 在 Win7 SP1 上是否可用** —— 这条路一次都没跑过。
     真机上删一个测试文件，确认它进了系统回收站而不是被直接删掉
- ⬜ **`wsMove` 的跨盘回退**（`EXDEV` → `copy + unlink`）与「目标只读」分支
  只在代码里走过。拿 U 盘或第二个分区构造一次
- ⬜ `ts.worker` 懒加载的转发逻辑（空壳 Worker + 队列补发）只在构建期验证过
  「不再进主 chunk」，**运行时行为没验**。手工验法：打开一个 `.js` 文件，
  确认补全与语法校验正常（退化表现是「像记事本」——没有高亮与括号匹配）

### 第三轮新增的未验证项（优先级从高到低）

第三轮改了 7 块功能，**全部没有在运行时点过**（沙箱限制）。
下面按「失败时是否无声」排序 —— 无声的排前面，因为它们最容易被漏过去。

- ⬜ **① 中文输出编码（最要紧）**：cmd 换掉了 PowerShell，
  脚本前缀从 `[Console]::OutputEncoding` 换成 `chcp 65001 >nul`。
  这条路径一次都没在 Windows 上跑过。
  **验法**：让学生脚本 `print("你好")`，或用 `runCommand` 跑
  `python -c "print('hello')"`（命令本身英文，但输出可能含中文）。
  看到 `����` 就说明 `chcp 65001` 没生效 —— 那时把 `shell.ts` 里的
  `chcp` 换成 `chcp 65001` 不带 `>nul` 看看，或改用 PowerShell 分支。
- ⬜ **② `where` 探测运行时**：`runtimes.ts` 靠 `where python` 找解释器。
  cmd 内置 `where` 是 Vista+ 就有的，但**校园精简版系统可能删了它**。
  验法：开一次「帮助 → 运行环境体检」，看「本机开发环境」那一段是否正确。
  如果空白但机器上确实装了 python，就是 `where` 不可用 ——
  那时退化成直接查几个常见安装路径。
- ⬜ **③ 预览面板的三栏宽度**：自检里 `previewOk` 会量三栏之和
  ≤ 内容区宽度（这是本轮最容易踩的坑：编辑器 + 对话已经占满 100%，
  预览再另占一块就溢出）。CI 能验到，但**真机窄屏下（1366×768）要再看一眼**。
- ⬜ **④ `!` + Tab 片段补全**：Monaco 补全菜单的实际交互没验过。
  验法：打开一个 .html，行首敲 `!`，看菜单是否弹出、按 Tab 是否展开。
  **如果 `!` 弹不出来**：`snippets.ts` 的 `isTriggeredAt` 依赖
  「光标前这一行」的原文，而 Monaco 的 `getWordUntilPosition` 对
  `!` 这种非单词字符在不同版本行为不一致 —— 那时改用
  `model.getLineContent()` 自己取（现在已经是这么做的，但值得复核）。
- ⬜ **⑤ 图片多模态**：中转站是否接受 `image_url` 内容块格式没验过。
  验法：设置里勾「支持图片输入」，贴一张截图发出去。
  **报 400 就优先怀疑中转站不吃这个格式**（有些只认 `content` 是字符串）。
- ⬜ **⑥ 拖到 `split=0.78` 时开预览**：share 会夹到 0.11，
  对话剩 19.6%。数学上验算过（8 种取值合计恒为 100%），
  但**实际观感**（19.6% 的对话栏够不够读）要人眼判断。
